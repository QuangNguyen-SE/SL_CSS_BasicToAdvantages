# CSS !important rule:
- `!important` là 1 keyword trong css giúp property đó đạt được trạng thái ưu tiên cao nhất
- Từ khóa này được đặt ở cuối lệnh property trong khai báo css
```css
selector {
  property: value !important;
}
```